interface:
  display_name: "1688 选品总控（兼容入口）"
  short_description: "历史调用名；显式调用后转交正式 1688 闭环"
  default_prompt: "Use $lc-product-selection-orchestrator to forward this request to $lc-1688-channel-closure."
policy:
  allow_implicit_invocation: false
